============
Installation
============

At the command line::

    $ easy_install django-nufiles

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-nufiles
    $ pip install django-nufiles